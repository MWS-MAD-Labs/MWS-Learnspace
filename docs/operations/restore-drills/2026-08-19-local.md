# Local restore drill — 2026-08-19

- **Operator:** coding agent on the maintainer workstation
- **Data classification:** synthetic development data only; no real student information
- **PostgreSQL:** `postgres:16-alpine`
- **Source database:** `learnspace_p2_012_source`
- **Restored database:** `learnspace_p2_012_restored`
- **Archive:** `backups/p2-012-local.dump` (removed after verification)
- **SHA-256:** `b2e2cc3efe411de17a3312b78fb504f8a952ab63d6b16907b5b770d5e4ae1a54`

## Procedure and observed results

1. Created the disposable source database without deleting or replacing the primary Compose volume.
2. Applied committed migration `20260819000000_initial` successfully.
3. Ran the guarded synthetic Prisma seed.
4. Added a deterministic probe table containing `alpha`, `beta`, and `gamma`.
5. Created a PostgreSQL custom-format archive and checksum with `npm run db:backup`.
6. Dropped only the disposable source database.
7. The first restore attempt exposed a checksum sidecar path bug; the script was corrected to write a basename-relative checksum.
8. Restored the archive into a new disposable database with `npm run db:restore`.
9. Verification returned:
   - probe rows: `3:alpha,beta,gamma`;
   - successful Prisma migrations: `1`;
   - seeded organization rows matching `learnspace-demo`: `1`.
10. Removed the disposable restored database and local archive after recording the result.

## Result

**Passed after one script correction.** The source database was deleted before restoration, the restored schema and fake data matched expectations, and the configured primary database and named Compose volume were not deleted.
